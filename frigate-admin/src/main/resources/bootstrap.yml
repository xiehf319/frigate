server:
  port: 6001
spring:
  application:
    name: frigate-admin
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: service-registry