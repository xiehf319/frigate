server:
  port: 4002
spring:
  application:
    name: id-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/work_node?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    username: root
    password: aaaaaa
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml