server:
  port: 5001
spring:
  application:
    name: user-center
  redis:
    host: localhost
    port: 6379
    database: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
security:
  access:
    expires: 3600
    enable-refresh: true
  refresh:
    expires: 7200
